---
"@palot/desktop": patch
---

Fix "Add Project" for remote servers: show a path text input dialog instead of the native folder picker (which only shows the local filesystem). Also makes the automation dialog's project picker remote-aware with an inline path input.
