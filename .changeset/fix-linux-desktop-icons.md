---
"@palot/desktop": patch
---

Fix missing app icon in Linux dock and app selector (GNOME/Fedora) by adding StartupWMClass to the desktop entry, setting an explicit executable name, fixing the BrowserWindow icon path for packaged builds, and providing a multi-size icon set
