---
"@palot/desktop": minor
---

Add configurable local server hostname, port, and password in settings. Harden server switching and discovery with health checks, health-probe non-active servers when the popover opens, and show empty states when the server is offline or no projects exist.
