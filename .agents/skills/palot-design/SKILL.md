---
name: palot-design
description: Apply Palot's design contract. Use when changing renderer UI, responsive layouts, panes, sidebars, cards, Tailwind classes, themes, typography, component styling, or visual interaction states.
slash: false
---

# Palot Design

Read `../../../DESIGN.md` before changing renderer UI.

Use existing semantic tokens and shared primitives. Design reusable surfaces around allocated space rather than device categories. Load `frontend-design` when creating or substantially reshaping the visual direction, and use `test-palot-desktop` for native visual QA.
